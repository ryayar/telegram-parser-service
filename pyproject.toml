[project]
name = "telegram-parser-service"
version = "0.1.0"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["shared*", "bot_api*", "userbot*"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
target-version = "py39"
line-length = 100
